apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: aeron

resources:
  - namespace.yaml
  - media-driver.yaml
  - archive.yaml
  - cluster.yaml

labels:
  - pairs:
      app.kubernetes.io/part-of: harus-aeron-zig
    includeSelectors: false
